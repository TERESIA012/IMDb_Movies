import { PopularMovieApiClass } from './popular-movie-api';

describe('PopularMovieApiClass', () => {
  it('should create an instance', () => {
    expect(new PopularMovieApiClass()).toBeTruthy();
  });
});
